apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "catawiki-playwright-automation.fullname" . }}-test-connection"
  labels:
    {{- include "catawiki-playwright-automation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "catawiki-playwright-automation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
